const defaultSectionSponsorData = {
  type: 0,
  hide: false,
  inherit: true,
};

export const createDefaultSectionSponsorData = () => ({
  sections: [
    defaultSectionSponsorData,
    defaultSectionSponsorData,
    defaultSectionSponsorData,
    defaultSectionSponsorData,
  ],
});
